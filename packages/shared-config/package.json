{
  "name": "@smoketech/shared-config",
  "version": "1.0.0",
  "description": "Shared configuration and presets for SmokeTech video generation system (viral engine + omega-platform)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy-config",
    "copy-config": "node -e \"const fs = require('fs'); const path = require('path'); const src = path.join('src', 'config'); const dest = path.join('dist', 'config'); if (!fs.existsSync(dest)) fs.mkdirSync(dest, { recursive: true }); fs.readdirSync(src).filter(f => f.endsWith('.json')).forEach(f => fs.copyFileSync(path.join(src, f), path.join(dest, f)));\"",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "smoketech",
    "viral",
    "omega-platform",
    "video-generation",
    "character-presets",
    "configuration"
  ],
  "author": "SmokeDev",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.8.0",
    "typescript": "^5.3.0",
    "jest": "^30.2.0",
    "@types/jest": "^30.0.0",
    "ts-jest": "^29.1.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
