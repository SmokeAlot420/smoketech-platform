{
  "name": "smoketech-platform",
  "version": "1.0.0",
  "private": true,
  "description": "SmokeTech AI Video Generation Platform - Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --names \"temporal,worker,frontend\" --prefix-colors \"blue,green,yellow\" \"cd apps/backend && ./temporal.exe server start-dev\" \"cd apps/backend && npm run worker\" \"turbo run dev --filter=openjourney-app\"",
    "dev:frontend": "turbo run dev --filter=openjourney-app",
    "dev:backend": "turbo run dev --filter=viral-ai-content-system",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "packageManager": "npm@10.0.0"
}
